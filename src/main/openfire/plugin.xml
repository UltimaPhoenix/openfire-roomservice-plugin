<?xml version="1.0" encoding="UTF-8"?>

<plugin>
    <class>org.jivesoftware.openfire.plugin.rooomservice.RoomServicePlugin</class>
    <!-- Plugin meta-data -->
	<name>${pom.name}</name>
	<description>${pom.description}</description>
	<author>${pom.organization.name}</author>
	<version>${pom.version}</version>
	<date>${openfire-plugin.build.date}</date>
	<minServerVersion>3.3.0</minServerVersion>
	<licenseType>gpl</licenseType>
    
    <adminconsole>		
        <tab id="tab-server">
            <sidebar id="sidebar-server-settings">
                <item id="room-service" name="Room Service" url="room-service.jsp"
                     description="Click to manage the service that allows remote admin of rooms" />
            </sidebar>
        </tab>
    </adminconsole>

</plugin>
